{
  "name": "phieu-thong-tin-tu-van-ios-navy-local-v3",
  "version": "1.2.0",
  "private": true,
  "main": "electron.js",

  "dependencies": {
    "@supabase/supabase-js": "^2.86.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "xlsx": "^0.18.5"
  },

  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "electron": "electron .",
    "dist": "npm run build && electron-builder"
  },

  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },

  "devDependencies": {
    "electron": "^39.2.6",
    "electron-builder": "^26.0.12",
    "eslint-plugin-react-hooks": "^7.0.1"
  },

  "build": {
    "appId": "vn.saohan.ptvd",
    "productName": "PTVD",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/**/*",
      "electron.js",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "public/favicon.ico"
    }
  }
}
